float a = 0, b = 0, c = 0;

int foo(int a, string b){
	print "ola";
}

int gcd(int m, float n) {

	float res = 0;
	if (m!=n) { 
	    if (m > n) 
	        res = gcd(foo(5, "ala"), 5.0, 4);
	    else
	        res = gcd(5, 4);
	    repeat {
	        m = 2 * 4;
	        n = 6 * m;
	    } until ((n > 666) || (m ^ 101001));
	}
	else
	    res = m;
	while(1) { 
		break; 
		return ala; 
		#(lubie placki);
	}
	
	print "ala";
	print (res);
	return res;
}

while(a >= b ) {
    a = 1/2*(a+b/a);
}
