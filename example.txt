float a = 0, b = 0, c = 0;

int gcd(int m, int n) {

	int res = 0;
	if (m!=n) { 
	    if (m > n) 
	        res = gcd(m-n, n);
	    else
	        res = gcd(n-m, m);
	    repeat {
	        m = 2 * n;
	        n = 6 * m;
	    } until ((n > "666") || (m ^ 101001));
	}
	else
	    res = m;
	while(1) { 
		break; 
		return ala; 
		#(lubie placki);
	}
	
	print "ala";
	print (res);
	return res;
}

while(a >= b ) {
    a = 1/2*(a+b/a);
}
